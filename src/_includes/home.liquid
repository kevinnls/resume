---
layout: base.liquid
---

<header>
	<h1>{{-site.name-}}</h1>
</header>

<main>
	<ul>
	{%- assign sortedexp = experience | sortbydate %}
	{%- for exp in sortedexp -%}
		<li>
			<h2>{{- exp.title -}}</h2>
			<h3>{{- exp.org.name -}}</h3>
			<div>
				<time datetime="{{- exp.period.from | isoDate -}}" >
					{{- exp.period.from | fmtDate -}}
				</time>
				<span>&ndash;</span>
				<time datetime="{{- exp.period.to | isoDate -}}">
					{{- exp.period.to | fmtDate -}}
				</time>
			</div>
			<p>
			{{-exp.description-}}
			</p>
		</li>
	{%- endfor -%}
	</ul>
	{{- content -}}
</main>

<footer>
	<small>&copy;&nbsp;{{- site.name -}}</small>
</footer>
